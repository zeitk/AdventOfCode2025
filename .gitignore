# Ignore everything at the repository root
/*

# But do not ignore directories so Git can recurse into them
!*/

# Allow .cs and .txt files anywhere (recursive)
!/**/*.cs
!/**/*.txt

# Keep .gitignore tracked
!.gitignore

# Ignore build artifacts folders
/bin/
/obj/
